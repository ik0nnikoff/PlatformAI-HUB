# Инструкции GitHub Copilot для PlatformAI-HUB

**ВСЕГДА отвечай на русском языке.** Используй Markdown для форматирования текста. Следуй инструкциям и шаблонам, приведенным ниже.

## 👨‍💻 ПРОФИЛЬ РАЗРАБОТЧИКА

**Ты senior developer** - вдумчивый, внимательный к мелочам, последовательный, думающий на перспективу. Применяешь лучшие практики разработки, следуешь принципам SOLID и создаешь масштабируемые решения.

## 🎯 СИСТЕМНЫЕ ТРЕБОВАНИЯ

### Оценка неопределённости
**Прежде чем ответить, оцени неопределённость своего ответа. Если она больше 0.1, задай мне уточняющие вопросы, пока она не станет 0.1 или ниже.**

### Принципы архитектуры и требования к коду

**Архитектурные принципы**:
- Объектно-ориентированное программирование (ООП)
- Четкое разделение ответственности между классами
- Логическое группирование функционала по файлам и каталогам
- Исключение дублирования кода
- Унифицированный стиль кодирования
- Профессиональный подход к реализации
- Консистентность во всем проекте
- Простота расширения и модификации функционала
- Удобство дополнения, редактирования и расширения функционала

**Принципы SOLID (ОБЯЗАТЕЛЬНЫЕ)**:
- **S - Single Responsibility**: Каждый класс отвечает только за одну задачу
- **O - Open/Closed**: Классы открыты для расширения, закрыты для модификации
- **L - Liskov Substitution**: Подклассы должны заменять базовые классы без нарушения логики
- **I - Interface Segregation**: Множество специализированных интерфейсов лучше одного универсального
- **D - Dependency Inversion**: Зависимости должны строиться на абстракциях, не на конкретных реализациях

## 🔧 ОБЯЗАТЕЛЬНОЕ ИСПОЛЬЗОВАНИЕ MCP СЕРВЕРОВ

**ПОДХОД SENIOR DEVELOPER**: Всегда начинай сложные задачи с планирования через MCP серверы.

**ОБЯЗАТЕЛЬНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ:**
1. **Sequential Thinking MCP** - структурированное планирование и анализ
2. **Memory MCP** - поиск существующих знаний о проекте  
3. **Context7 MCP** - изучение документации библиотек
4. **Codacy MCP** - анализ качества кода (обязательно для всех изменений)
5. **Playwright MCP** - автоматизация браузера и тестирование

### Краткий обзор MCP серверов:

#### Sequential Thinking MCP
**Когда использовать**: Сложные архитектурные решения, отладка системных проблем, рефакторинг
**Принципы**:
- Начинай с анализа проблемы и контекста
- Рассматривай альтернативы и их влияние
- Пересматривай решения при появлении новой информации
- Используй ветвление для альтернативных подходов

#### Memory MCP  
**Когда использовать**: Сохранение архитектурных решений, документирование найденных решений
**Принципы**:
- Создавай связи между компонентами системы
- Документируй причины архитектурных решений
- Сохраняй решения проблем для повторного использования
- Ищи существующие решения перед созданием новых
- Обращайся к Memory MCP для получения контекста проекта и восстановления памяти

#### Context7 MCP
**Когда использовать**: Работа с библиотеками, изучение паттернов, примеров, документации
**Принципы**:
- Изучай документацию ПЕРЕД использованием библиотек
- Ищи лучшие практики и паттерны
- Используй для поиска конкретных решений
- Изучай примеры использования библиотек
- Фокусируйся на конкретных задачах (async patterns, error handling)

#### Codacy MCP
**Когда использовать**: ОБЯЗАТЕЛЬНО при любых изменениях кода
**Принципы**:
- Анализируй качество кода после каждого изменения
- Проверяй безопасность (SAST, secrets scanning)
- Отслеживай метрики покрытия тестами
- Настраивай автоматические проверки

#### Playwright MCP  
**Когда использовать**: E2E тестирование, автоматизация веб-процессов
**Принципы**:
- Используй resilient locators (getByRole, getByText)
- Применяй web-first assertions с автоматическим ожиданием
- Изолируй тесты с beforeEach hooks
- Мокай внешние зависимости

## Обзор проекта
Платформа управления AI агентами на базе FastAPI с LangGraph, SQLAlchemy (async), PostgreSQL, Redis и мульти-платформенными интеграциями (Telegram, WhatsApp). Поддерживает голосовую обработку с STT/TTS провайдерами (OpenAI, Google, Yandex).

**Версия**: 0.1.0  
**Python**: >= 3.12  
**Менеджер пакетов**: uv (заменяет pip/poetry)  
**База данных**: PostgreSQL с AsyncPG  
**Кэш**: Redis для Pub/Sub и состояний  
**Хранилище файлов**: MinIO для голосовых файлов

## Актуальная архитектура проекта

### Текущая структура модулей
- **app/main.py**: FastAPI приложение с lifespan управлением
- **app/core/**: Конфигурация, логирование, зависимости, базовые компоненты
- **app/api/routers/**: REST API endpoints (agent_api, chat_api, integration_api, user_api, websocket_api, sse_api)
- **app/db/**: База данных (alchemy_models, session, crud/)
- **app/agent_runner/**: Система запуска агентов (agent_runner.py, runner_main.py, langgraph/, common/)
- **app/integrations/**: Интеграции (telegram/, whatsapp/)
- **app/services/**: Бизнес-логика (process_manager, redis_service, websocket_manager, voice/)
- **app/workers/**: Фоновые задачи (history_saver_worker, token_usage_worker, inactivity_monitor_worker)

## Архитектурные паттерны

### Паттерн Service Component Base
Все долгоживущие сервисы наследуются от `ServiceComponentBase` (`app/core/base/service_component.py`):
- **AgentRunner**: Управляет жизненным циклом LangGraph агентов, слушает Redis Pub/Sub
- **Integration Bots**: WhatsApp/Telegram боты с паттернами Socket.IO/webhook  
- **Workers**: Фоновые процессоры (HistorySaverWorker, TokenUsageWorker, InactivityMonitorWorker)

Ключевой паттерн: Регистрируй асинхронные задачи через `_register_main_task()`, реализуй `_handle_pubsub_message()` для Redis коммуникации.

### Paттерн ProcessManager
Централизованное управление процессами через `ProcessManager`:
- Запуск/остановка агентов через subprocess 
- Отслеживание статусов в Redis
- Автоматический перезапуск при сбоях
- Координация агентов и интеграций

### Архитектура Redis коммуникации
- **Ввод агента**: `agent:{agent_id}:input` - Сообщения агенту
- **Вывод агента**: `agent:{agent_id}:output` - Ответы агента  
- **Ключи статуса**: `agent_status:{agent_id}`, `integration_status:{agent_id}:{type}`
- **Очереди**: `history_queue`, `token_usage_queue` для фоновой обработки
- **Кэш**: STT результаты, пользовательские данные с TTL

### Паттерн зависимостей FastAPI
Все зависимости определены в `app/core/dependencies.py`:
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]:
    """FastAPI зависимость для БД сессии"""
    
async def get_redis_client() -> AsyncGenerator[redis.Redis, None]:
    """FastAPI зависимость для Redis клиента"""
    
async def get_process_manager() -> ProcessManager:
    """Зависимость для ProcessManager"""
```

### Паттерны базы данных
Следуй строгим асинхронным паттернам в `app/db/crud/`:
```python
async def crud_function(db: AsyncSession, ...):
    logger.debug(f"Описание операции с параметрами")
    try:
        # операция
        await db.commit()
        await db.refresh(obj) # если нужно
        logger.debug(f"Сообщение об успехе")
        return result
    except SQLAlchemyError as e:
        await db.rollback()
        logger.error(f"Сообщение об ошибке: {e}", exc_info=True)
        raise
    except Exception as e:
        await db.rollback()
        logger.error(f"Unexpected error: {e}", exc_info=True)
        raise
```

## Основные рабочие процессы разработчика

### Команды разработки
```bash
make run                    # Запуск FastAPI сервера (порт 8001)
make migrate               # Применение Alembic миграций
make makemigrations m="..."# Создание новой миграции
make downgrade             # Откат последней миграции
make history               # Просмотр истории миграций
make current               # Просмотр текущей ревизии
make run_agent AGENT_ID=x  # Тестирование конкретного агента
make stop_agent AGENT_ID=x # Остановка агента через API
make stop_all_runners      # Остановка всех агентов
make run_telegram_bot AGENT_ID=x BOT_TOKEN=y # Запуск Telegram бота
```

### Команды uv (менеджер пакетов)
```bash
uv sync                    # Синхронизация зависимостей с uv.lock
uv add package_name        # Добавить новую зависимость
uv remove package_name     # Удалить зависимость
uv run command             # Запуск команды в виртуальном окружении
uv pip install package    # Установка пакета (альтернатива pip)
```

### Структура миграций БД
```bash
alembic revision --autogenerate -m "описание"  # Создать миграцию
alembic upgrade head                           # Применить все миграции
alembic downgrade -1                          # Откатить одну миграцию
alembic history                               # История миграций
alembic current                               # Текущая ревизия
```

### Конфигурация API
- **FastAPI Server**: `http://localhost:8001`
- **API Base URL**: `http://localhost:8001/api/v1`
- **Документация**: `http://localhost:8001/docs`
- **OpenAPI JSON**: `http://localhost:8001/api/v1/openapi.json`

### Актуальные API endpoints
```
GET    /api/v1/agents                    # Список агентов
POST   /api/v1/agents                    # Создать агента
GET    /api/v1/agents/{id}               # Получить агента
PUT    /api/v1/agents/{id}               # Обновить агента
DELETE /api/v1/agents/{id}               # Удалить агента
POST   /api/v1/agents/{id}/start         # Запустить агента
POST   /api/v1/agents/{id}/stop          # Остановить агента
GET    /api/v1/agents/{id}/status        # Статус агента

# Интеграции
POST   /api/v1/agents/{id}/integrations/{type}/start  # Запустить интеграцию
POST   /api/v1/agents/{id}/integrations/{type}/stop   # Остановить интеграцию
GET    /api/v1/agents/{id}/integrations/{type}/status # Статус интеграции

# Чаты
GET    /api/v1/chat/{agent_id}/chats     # Список чатов агента
GET    /api/v1/chat/{agent_id}/chats/{thread_id} # История чата
DELETE /api/v1/chat/{agent_id}/chats/{thread_id} # Удалить чат

# Пользователи
GET    /api/v1/users                     # Список пользователей

# WebSocket & SSE
WS     /api/v1/ws/agents/{id}            # WebSocket для агента
GET    /api/v1/agents/{id}/sse           # Server-Sent Events
```

### Интеграция голосовых сервисов
Голосовые возможности настраиваются для каждого агента в `config.simple.settings.voice_settings`:
- Мульти-провайдерная система fallback (OpenAI → Google → Yandex)
- Обнаружение намерений с сопоставлением ключевых слов
- STT/TTS обработка через `VoiceServiceOrchestrator`
- Обработка аудиофайлов с хранением в MinIO

**Аутентификация Yandex SpeechKit**: Использует API Key (`YANDEX_API_KEY`) аутентификацию, НЕ IAM Token. IAM Token аутентификация устарела для этого проекта.

### Паттерны тестирования
- Юнит-тесты для отдельных компонентов в `tests/`
- Интеграционные тесты покрывают полные потоки сообщений
- Mock Redis/DB соединения с правильными async context managers
- Голосовые тесты включают конвертацию аудиоформатов (OGG→WAV)

### Система лицензирования
В проекте используется открытая архитектура:
- **Основная платформа**: MIT/Apache 2.0 совместимые библиотеки
- **LangGraph**: LangGraph License (коммерческое использование требует лицензии)
- **Провайдеры LLM**: Требуют собственные API ключи (OpenAI, Anthropic, etc.)
- **Внешние сервисы**: WhatsApp (wppconnect), Telegram (Bot API)

## Структура конфигурации

### Схема конфигурации агента
```json
{
  "config": {
    "simple": {
      "settings": {
        "integrations": [{"type": "TELEGRAM|WHATSAPP", "settings": {...}}],
        "voice_settings": {
          "enabled": true,
          "providers": [{"provider": "openai", "priority": 1, ...}]
        }
      }
    }
  }
}
```

### Зависимости окружения
Критически важные переменные окружения: `DATABASE_URL`, `REDIS_URL`, `OPENAI_API_KEY`. Голосовые сервисы требуют ключи для конкретных провайдеров:
- **Yandex SpeechKit**: `YANDEX_API_KEY` (основной), `YANDEX_FOLDER_ID` (обязательный)
- **Google Cloud**: `GOOGLE_APPLICATION_CREDENTIALS`, `GOOGLE_CLOUD_PROJECT_ID`
- **OpenAI**: Использует существующий `OPENAI_API_KEY`

**Важно**: Аутентификация Yandex использует метод API Key, не IAM Token.

### Конфигурация настроек (app/core/config.py)
Все настройки проекта централизованы в `Settings` класс:
```python
class Settings:
    # Основные настройки
    PROJECT_NAME: str = "PlatformAI Hub"
    PROJECT_VERSION: str = "0.1.0" 
    API_V1_STR: str = "/api/v1"
    
    # Сервер
    SERVER_HOST: str = "localhost"
    SERVER_PORT: int = 8001
    DEBUG: bool = True
    
    # База данных и Redis
    DATABASE_URL: str = os.getenv("DATABASE_URL")
    REDIS_URL: str = os.getenv("REDIS_URL")
    
    # Настройки агентов
    AGENT_RUNNER_MODULE_PATH: str = "app.agent_runner.runner_main"
    AGENT_INACTIVITY_TIMEOUT: int = 1800  # 30 минут
    
    # Голосовые сервисы
    VOICE_MAX_DURATION: int = 120
    VOICE_MAX_FILE_SIZE_MB: int = 25
    MINIO_ENDPOINT: str = "localhost:9000"
```

## Алгоритм общения пользователей с агентами

### Схема коммуникации
```
Пользователь → Интеграция (WhatsApp/Telegram) → Redis Pub/Sub → AgentRunner → LangGraph → Ответ
```

### Детальный процесс:
1. **Получение сообщения**: Интеграция получает сообщение через Socket.IO (WhatsApp) или Bot API (Telegram)
2. **Обработка пользователя**: Создание/обновление пользователя в БД, проверка авторизации
3. **Публикация в Redis**: Сообщение публикуется в канал `agent:{agent_id}:input`
4. **AgentRunner**: Получает сообщение, формирует контекст для LangGraph
5. **LangGraph обработка**: Агент обрабатывает запрос через настроенные tools и nodes
6. **Ответ агента**: Результат публикуется в `agent:{agent_id}:output`
7. **Отправка пользователю**: Интеграция получает ответ и отправляет пользователю

### Структура payload для агента:
```json
{
  "text": "сообщение пользователя",
  "chat_id": "id чата",
  "platform_user_id": "id пользователя на платформе",
  "user_data": {"user_info": "...", "is_authenticated": true},
  "channel": "telegram|whatsapp",
  "image_urls": ["url1", "url2"],  // опционально
  "voice_data": {...},             // опционально
  "document_content": "...",       // опционально
  "document_metadata": {...}       // опционально
}
```

## Функционал обработки медиа-файлов

### Архитектура медиа-обработки
Проект использует оркестраторы для централизованного управления медиа-контентом:
- **VoiceServiceOrchестrator** (`app/services/voice/voice_service_orchestrator.py`)
- **ImageOrchестrator** (`app/services/media/image_orchestrator.py`)
- **DocumentOrchестrator** (`app/services/media/document_orchestrator.py`)

Все оркестраторы следуют паттерну мульти-провайдерной архитектуры с fallback механизмами.

### Голосовые сообщения
**Архитектура**: VoiceServiceOrчестrator с мульти-провайдерной системой (OpenAI → Google → Yandex)

**Компоненты системы**:
- **OpenAIVoiceService**: STT/TTS через OpenAI Whisper и TTS API
- **GoogleVoiceService**: Google Cloud Speech-to-Text и Text-to-Speech
- **YandexVoiceService**: Yandex SpeechKit с API Key аутентификацией
- **Аудио конвертация**: pydub для преобразования OGG→WAV (совместимость с провайдерами)

**Алгоритм обработки**:
1. Интеграция получает голосовое сообщение (OGG, MP3, WAV, OPUS, FLAC, AAC)
2. VoiceServiceOrчестrator загружает файл в MinIO bucket `voice-files`
3. Аудио конвертируется в WAV через pydub для совместимости
4. STT обработка с fallback: OpenAI → Google → Yandex (по приоритету)
5. Результат кэшируется в Redis (TTL 24 часа) и отправляется агенту
6. При необходимости TTS ответа: обратная цепочка провайдеров
7. Аудио ответ загружается в MinIO и отправляется пользователю

**Методы VoiceServiceOrчестrator**:
- `transcribe_audio(file_path, language="auto")`: STT с автоопределением языка
- `synthesize_speech(text, voice_settings)`: TTS с настройками голоса
- `get_supported_formats()`: Список поддерживаемых аудио форматов
- `convert_audio_format(input_path, output_format)`: Конвертация форматов

**Кэширование**: Redis ключи `stt_cache:{file_hash}` и `tts_cache:{text_hash}` с TTL 24 часа

### Изображения  
**Архитектура**: ImageOrчестrator с Vision API провайдерами (OpenAI GPT-4V → Google Vision → Claude Vision)

**Компоненты системы**:
- **OpenAIVisionProvider**: GPT-4V для анализа изображений
- **GoogleVisionProvider**: Google Cloud Vision API 
- **ClaudeVisionProvider**: Anthropic Claude Vision
- **MinIOImageManager**: Управление загрузкой и presigned URLs

**Алгоритм обработки**:
1. Интеграция получает изображение (JPEG, PNG, GIF, WebP)
2. ImageOrchестrator загружает в MinIO bucket `images`
3. Генерируется presigned URL с TTL 1 час для безопасного доступа
4. URL передается агенту в поле `image_urls` через Redis payload
5. Агент вызывает `analyze_images` tool с analysis_prompt
6. Vision API анализ с fallback по провайдерам при ошибках
7. Результат анализа включается в ответ агента пользователю

**Методы ImageOrчестrator**:
- `process_images(images_data, agent_id, user_id)`: Загрузка изображений в MinIO и генерация presigned URLs
- `analyze_images(image_urls, prompt)`: Анализ изображений через Vision API с fallback
- `process_and_analyze_images()`: Полный цикл обработки и анализа
- `upload_user_image()`: Загрузка одного изображения с генерацией presigned URL
- `health_check()`: Проверка состояния провайдеров и MinIO connectivity

**LangGraph Tool**: `analyze_images(image_urls: List[str], analysis_prompt: str)` - анализ через Vision API

### Документы
**Архитектура**: DocumentOrчестrator с Marker API (PDF, image, PPT, PPTX, DOC, DOCX, XLS, XLSX, HTML, EPUB files →Markdown) и AI fallback провайдерами

**Компоненты системы**:
- **MarkerAPIProvider**: Высококачественная конвертация Документ→Markdown через Marker API
- **AIFallbackProvider**: OpenAI/Claude fallback при недоступности Marker
- **MinIODocumentManager**: Управление загрузкой документов и результатов
- **DocumentProcessingCache**: Redis кэширование результатов конвертации

**Поддерживаемые форматы**: PDF, DOC, DOCX, XLS, XLSX, CSV, TXT, MD, PPTX, EPUB, HTML
**Лимит размера**: 20MB (настройка DOCUMENT_MAX_SIZE_MB в .env)

**Алгоритм обработки**:
1. Интеграция получает документ через upload endpoint
2. DocumentOrчестrator валидирует формат и размер файла
3. Загрузка в MinIO bucket `user-files` с уникальным именем
4. Конвертация в Markdown:
   - **Приоритет 1**: Marker API - высокое качество конвертации
   - **Fallback**: AI провайдеры (OpenAI/Claude) для всех форматов
5. Результат кэшируется в Redis (TTL 24 часа) с ключом `doc_cache:{file_hash}`
6. Markdown контент передается агенту в поле `document_content`
7. Агент обрабатывает через document tools
8. Сгенерированные документы сохраняются в MinIO для download

**Методы DocumentOrчестrator**:
- `process_user_document(document_data, agent_id, user_id)`: Полная обработка документа пользователя с валидацией, загрузкой в MinIO и конвертацией в Markdown
- `get_document_content(document_info)`: Получение Markdown содержимого документа
- `generate_document(content, agent_id, user_id, format)`: Создание новых документов (PDF/DOCX)
- `edit_document(document_info, instructions, new_format)`: AI-редактирование существующих документов
- `get_user_documents(agent_id, user_id)`: Список документов пользователя
- `delete_document(document_info)`: Удаление документа из MinIO
- `health_check()`: Проверка состояния всех провайдеров и MinIO connectivity

**LangGraph Tools (7 инструментов)**:
- `process_document_tool`: Анализ и извлечение информации
- `edit_document_tool`: Редактирование содержимого  
- `generate_document_tool`: Создание новых документов
- `convert_document_format_tool`: Конвертация между форматами
- `extract_document_metadata_tool`: Извлечение метаданных
- `search_document_content_tool`: Поиск по содержимому
- `summarize_document_tool`: Создание резюме документа

**Кэширование**: Redis ключи `doc_cache:{file_hash}` и `doc_metadata:{doc_id}` с TTL 24 часа

## LangGraph Tools Architecture

### Архитектура агентов LangGraph
**Основной файл**: `app/agent_runner/langgraph/factory.py` - фабрика для создания агентов
**Структура workflow**: Граф из nodes, edges и conditional routing

### Ключевые компоненты:
- **AgentState**: Shared state между всеми nodes в workflow
- **Nodes**: Функциональные блоки (safe_tools_node, datastore_node, auth_node)
- **Edges**: Связи между nodes с conditional логикой
- **Tools**: Инструменты с dependency injection через InjectedState

### Структура Agent State:
```python
class AgentState(TypedDict):
    messages: Annotated[Sequence[BaseMessage], add_messages]
    user_data: Dict[str, Any]
    chat_id: str
    platform_user_id: str
    channel: str
    image_urls: List[str]
    voice_data: Optional[Dict]
    document_content: Optional[str]
    document_metadata: Optional[Dict]
```

### Архитектура Nodes и Edges:
**Nodes в workflow**:
- **auth_node**: Проверка авторизации пользователя
- **safe_tools_node**: Выполнение безопасных tools (voice, vision, documents)
- **datastore_node**: Работа с базами знаний и retrieval
- **end_node**: Завершение workflow с ответом

**Conditional Edges**:
- `should_continue`: Решение о продолжении или завершении
- `auth_check`: Маршрутизация после проверки авторизации
- `tools_condition`: Выбор между safe_tools и datastore

### Типы tools в проекте:
- **Internal tools**: voice_capabilities_tool, auth_tool, get_user_info_tool
- **Vision tools**: analyze_images 
- **Document tools**: process_document_tool, edit_document_tool, generate_document_tool, convert_document_format_tool, extract_document_metadata_tool, search_document_content_tool, summarize_document_tool
- **Knowledge Base tools**: retriever tools для RAG
- **API request tools**: настраиваемые HTTP запросы

### Паттерн создания tools:
```python
@tool
def tool_name(
    param: Annotated[Type, "description"],
    state: Annotated[Dict, InjectedState] = None
) -> str:
    """Tool description for LLM"""
    # Access to shared state
    user_data = state.get("user_data", {})
    chat_id = state.get("chat_id")
    
    # Implementation
    return result
```

### Dependency Injection через InjectedState:
- Tools получают доступ к AgentState через `state: Annotated[Dict, InjectedState]`
- Это позволяет tools'ам получать context: user_data, chat_id, voice_data, etc.
- State обновляется между calls и доступен всем nodes

### Интеграция в workflow:
- Tools добавляются в `configure_tools()` в factory.py
- Safe tools выполняются в safe_tools_node
- Datastore tools связаны с datastore_node
- Conditional edges определяют маршрутизацию через `should_continue()`
- Error handling через try/catch в nodes

### Lifecycle агента:
1. **Создание**: Agent factory создает граф с nodes и edges
2. **Инициализация**: Загрузка tools и настройка state
3. **Выполнение**: Message processing через workflow
4. **Маршрутизация**: Conditional edges определяют путь
5. **Завершение**: End node формирует ответ пользователю

## Process Management
`ProcessManager` handles agent/integration lifecycle:
- Subprocess spawning with proper Python module paths
- Redis status tracking and health monitoring
- Auto-restart on failures with configurable retry limits
- Integration coordination (start agents before integrations)

## Critical Dependencies
- **uv**: Package management (заменяет pip/poetry) 
- **Alembic**: Database migrations with async support
- **LangGraph**: Agent workflow orchestration
- **pydub**: Audio format conversion for voice messages
- **wppconnect-server**: External WhatsApp service (Socket.IO + HTTP)

## Documentation Structure
- `MANUAL/VOICE_*.md`: Comprehensive voice services documentation
- API schemas in `app/api/schemas/` with validation patterns
- Database models in `app/db/alchemy_models.py` with proper relationships

## File Organization Rules
- **Documentation**: MD files should be created in `MD/` folder with sequential numbering (1_, 2_, 3_, etc.)
- **Testing**: Test files should be created in `tests/` folder
- Follow established project structure

## Архитектурные принципы проекта
- CRUD операции в `app/db/crud/`
- Pydantic схемы в `app/api/schemas/`
- Роутеры в `app/api/routers/`
- Бизнес-логика в `app/services/`
- Конфигурация в `app/core/config.py`

## Детальное описание MCP серверов

### Memory MCP - Граф знаний проекта
**Назначение**: Персистентная память через локальный граф знаний для долгосрочного хранения информации между сессиями.

**Архитектура Knowledge Graph**:
- **Entities** - узлы с уникальным именем, типом и наблюдениями
- **Relations** - направленные связи между сущностями (активный залог)  
- **Observations** - атомарные факты, привязанные к сущностям

**Ключевые функции (9 инструментов)**:
- `create_entities`: создание множественных сущностей с observations
- `create_relations`: установление направленных связей (активный залог)
- `add_observations`: добавление новых фактов к существующим сущностям
- `delete_entities`: каскадное удаление сущностей и связей
- `delete_observations`: удаление конкретных наблюдений
- `delete_relations`: удаление специфичных связей
- `read_graph`: получение полной структуры графа
- `search_nodes`: поиск по именам, типам и содержимому observations
- `open_nodes`: извлечение конкретных узлов по именам

**Стратегия использования для PlatformAI-HUB**:

```markdown
1. СРАЗУ ПРИ НАЧАЛЕ сессии:
   - search_nodes("PlatformAI-HUB") для получения контекста проекта
   - read_graph() для обзора всех накопленных знаний

2. Создание архитектурных сущностей:
   - name: "FastAPI_Server", type: "service_component"
   - name: "LangGraph_Agent_Factory", type: "core_module"  
   - name: "Redis_PubSub_System", type: "communication_layer"
   - name: "MinIO_File_Storage", type: "storage_service"

3. Установление системных связей:
   - "AgentRunner" → "depends_on" → "Redis_PubSub_System"
   - "DocumentOrchestrator" → "integrates_with" → "MinIO_File_Storage"
   - "Telegram_Bot" → "communicates_through" → "Redis_PubSub_System"

4. Документирование решений:
   - Сущность: name="Voice_Fallback_Architecture", type="design_pattern"
   - Observation: "OpenAI → Google → Yandex priority chain for STT/TTS"
   - Relation: "VoiceServiceOrchestrator" → "implements" → "Voice_Fallback_Architecture"

5. Отслеживание проблем и решений:
   - name="Document_Tools_Duplication_Issue", type="technical_debt"
   - observations: ["Found duplicate tools in old vs new architecture", "Legacy files cleanup required"]
   - relations: "Document_Tools_Duplication_Issue" → "resolved_by" → "Architecture_Cleanup_Jan2025"
```

**Паттерны именования (критично для поиска)**:
- Компоненты: `ComponentName_Type` (FastAPI_Server, LangGraph_Agent)
- Проблемы: `Issue_Description_Date` (Memory_Leak_Jan2025)
- Решения: `Solution_Context_Version` (Fallback_Architecture_V2)
- Фичи: `Feature_Name_Status` (Voice_Processing_Implemented)

**Observations должны быть атомарными**:
- ✅ "Uses Redis Pub/Sub for agent communication"
- ✅ "Supports fallback chain: OpenAI → Google → Yandex"
- ❌ "Complex system with multiple integrations and fallbacks" (слишком общо)

### Context7 MCP - Документация библиотек
**Назначение**: Доступ к актуальной документации популярных библиотек и фреймворков.

**Когда использовать**:
- Работа с новыми библиотеками (FastAPI, LangGraph, SQLAlchemy и др.)
- Поиск лучших практик для фреймворков
- Получение свежей документации без поиска в интернете
- Решение проблем с API библиотек
- Изучение паттернов использования библиотек в проекте

**Ключевые функции**:
- `resolve-library-id`: поиск библиотек по названию
- `get-library-docs`: получение документации для конкретной библиотеки

**Лучшие практики использования**:
```markdown
1. ВСЕГДА проверяй документацию перед использованием новой библиотеки:
   - FastAPI → изучи dependency injection patterns
   - LangGraph → понимай node/edge архитектуру
   - SQLAlchemy → async patterns и best practices

2. Ищи специфичные темы:
   - "async patterns" для async/await библиотек
   - "configuration" для setup библиотек
   - "error handling" для обработки ошибок

3. Используй Context7 для решения конкретных проблем:
   - Как настроить connection pooling в SQLAlchemy
   - Лучшие практики для LangGraph state management
   - FastAPI dependency injection patterns
```

### Sequential Thinking MCP - Структурированное решение проблем
**Назначение**: Пошаговый анализ сложных технических задач с возможностью пересмотра подхода.

**Когда использовать**:
- Планирование архитектуры для новых функций
- Отладка сложных багов и системных проблем
- Рефакторинг больших разделов кода
- Принятие технических решений с множественными факторами
- Анализ производительности и оптимизация
- Проектирование новых интеграций

**Особенности**:
- Адаптивное планирование количества шагов
- Возможность пересмотра предыдущих решений
- Ветвление в решении проблем
- Генерация и проверка гипотез

**Лучшие практики использования**:
```markdown
1. НАЧИНАЙ СЛОЖНЫЕ ЗАДАЧИ с Sequential Thinking:
   - Планирование новых features
   - Отладка системных проблем
   - Архитектурные решения

2. Используй для структурированного анализа:
   - Шаг 1: Анализ проблемы и контекста
   - Шаг 2: Рассмотрение альтернатив
   - Шаг 3: Оценка рисков и преимуществ
   - Шаг 4: Выбор решения и plan implementation

3. Пересматривай решения при появлении новой информации:
   - Используй revision_thought для корректировки
   - Создавай branch_thought для альтернативных подходов
   - Не бойся изменить план при необходимости
```

### Codacy MCP - Анализ качества кода
**Назначение**: Интеграция с Codacy для анализа качества кода и безопасности.

**Когда использовать**:
- Анализ качества кода в репозитории
- Поиск уязвимостей безопасности (SAST, SCA, Secrets)
- Получение метрик покрытия тестами
- Настройка CI/CD пайплайнов
- **ОБЯЗАТЕЛЬНО используй при любых изменениях кода**
- **АКТИВНО используй для проверки нового функционала**
- Проверка соответствия стандартам кодирования

**Ключевые функции**:
- `codacy_cli_analyze`: локальный анализ через CLI
- `codacy_get_repository_with_analysis`: получение анализа репозитория
- `codacy_list_repository_issues`: список проблем качества кода
- `codacy_search_repository_srm_items`: поиск уязвимостей безопасности

**Лучшие практики использования**:
```markdown
1. ОБЯЗАТЕЛЬНЫЙ анализ кода:
   - После каждого изменения кода
   - Перед merge в основную ветку
   - При release preparation

2. Типы анализа:
   - Code Quality: стиль, сложность, дублирование
   - Security Issues: SAST, secrets scanning, SCA
   - Coverage Metrics: покрытие тестами

3. Интеграция в workflow:
   - Используй в pre-commit hooks
   - Настрой автоматические проверки в CI
   - Отслеживай метрики качества в dashboards
```

### Playwright MCP - Автоматизация браузера
**Назначение**: Автоматизация веб-браузера для тестирования и скрейпинга.

**Когда использовать**:
- Создание end-to-end тестов для веб-интерфейсов
- Автоматизация веб-интерфейсов и процессов
- Генерация скриншотов для документации
- Тестирование интеграций с веб-сервисами
- Валидация пользовательских workflow

**Ключевые функции**:
- `browser_navigate`: навигация по веб-страницам
- `browser_click`: клики по элементам
- `browser_take_screenshot`: создание скриншотов
- `browser_type`: ввод текста в формы
- `browser_wait_for`: ожидание элементов или событий

**Лучшие практики использования**:
```markdown
1. E2E тестирование:
   - Тестирование критических user journeys
   - Проверка интеграций между компонентами
   - Валидация workflow пользователей

2. Автоматизация рутинных задач:
   - Скрейпинг данных с веб-сайтов
   - Автоматизация административных задач
   - Создание демонстрационного контента

3. Мониторинг и валидация:
   - Healthcheck веб-интерфейсов
   - Снятие скриншотов для документации
   - Проверка доступности внешних сервисов
```

## Мульти-провайдерная архитектура

### Принципы fallback системы
Все медиа-оркестраторы используют единую логику приоритетов:
1. **Priority-based routing**: Провайдеры упорядочены по приоритету (1 = высший)
2. **Automatic fallback**: При ошибке автоматическое переключение на следующий провайдер
3. **Error handling**: Логирование ошибок и метрики по каждому провайдеру
4. **Circuit breaker**: Временное отключение провайдера при множественных ошибках

### Конфигурация провайдеров
**Пример для голосовых сервисов**:
```json
{
  "voice_settings": {
    "enabled": true,
    "providers": [
      {"provider": "openai", "priority": 1, "enabled": true},
      {"provider": "google", "priority": 2, "enabled": true},
      {"provider": "yandex", "priority": 3, "enabled": true}
    ]
  }
}
```

### Обработка ошибок провайдеров
- **Network errors**: Немедленное переключение на следующий провайдер
- **Rate limits**: Retry с exponential backoff, затем fallback
- **Authentication errors**: Пропуск провайдера и переход к следующему
- **Service unavailable**: Временное отключение провайдера на 5 минут

## MinIO интеграция и файловое хранилище

### Архитектура bucket'ов
- **voice-files**: Аудио файлы (OGG, WAV, MP3)
- **documents**: Исходные документы и сгенерированные файлы
- **images**: Загруженные изображения
- **temp**: Временные файлы с TTL 24 часа

### Presigned URLs безопасность
**Принципы**:
- Все файлы доступны только через presigned URLs (не прямой доступ)
- TTL по умолчанию: 1 час для изображений, 24 часа для документов
- Автоматическая ротация URLs при истечении времени
- Разделение чтения и записи (separate read/write URLs)

**Методы MinIOManager**:
```python
async def upload_file(bucket: str, object_name: str, file_data: bytes) -> str
async def generate_presigned_url(bucket: str, object_name: str, expires_hours: int = 1) -> str
async def delete_file(bucket: str, object_name: str) -> bool
async def list_files(bucket: str, prefix: str = "") -> List[str]
```

### Жизненный цикл файлов
1. **Upload**: Загрузка в соответствующий bucket с уникальным именем
2. **Processing**: Обработка через медиа-оркестраторы
3. **Caching**: Кэширование метаданных в Redis
4. **Access**: Доступ через presigned URLs с TTL
5. **Cleanup**: Автоматическое удаление по расписанию или вручную

### Интеграция с Redis кэшированием
- **Метаданные файлов**: `file_meta:{bucket}:{object_name}` (TTL 24h)
- **Presigned URLs**: `presigned_url:{bucket}:{object_name}` (TTL 45 min)
- **Processing results**: `processing_cache:{file_hash}` (TTL 24h)
- **File mapping**: `file_mapping:{original_name}` → `{bucket}/{object_name}`
